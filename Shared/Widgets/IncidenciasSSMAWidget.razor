@using WorkFlow_SIG10._1.Data
@using Microsoft.EntityFrameworkCore
@inject IDbContextFactory<ApplicationDbContext> DbContextFactory

<a href="/incidencias-ssma" class="widget-link">
    <div class="widget-header">
        <h5>Incidencias SSMA</h5>
        <span class="oi oi-warning widget-icon"></span>
    </div>
    <div class="widget-body">
        <div class="widget-value">@count</div>
        <div class="widget-description">incidencias registradas</div>
    </div>
</a>

@code {
    private int count;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            using var dbContext = await DbContextFactory.CreateDbContextAsync();
            count = await dbContext.IncidenciasSSMA.CountAsync();
        }
        catch (Exception)
        {
            // Log or handle the exception
            count = 0;
        }
    }
}