@using WorkFlow_SIG10._1.Data
@using Microsoft.EntityFrameworkCore
@inject IDbContextFactory<ApplicationDbContext> DbContextFactory

<div class="card text-white bg-secondary mb-3" style="max-width: 18rem;">
    <div class="card-header">Oficina Técnica</div>
    <div class="card-body">
        <h5 class="card-title">Total: @count</h5>
        <p class="card-text">Número total de documentos en oficina técnica.</p>
    </div>
</div>

@code {
    private int count;

    protected override async Task OnInitializedAsync()
    {
        using var dbContext = DbContextFactory.CreateDbContext();
        count = await dbContext.OficinasTecnicas.CountAsync();
    }
}
